import {
    StyleSheet,
} from 'react-native';

export const PageStyles = StyleSheet.create({
    container : {
        flex: 1,
        flexDirection: 'column',
        backgroundColor: 'darkslategrey',
    },
});
